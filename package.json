{
  "name": "recipes-backend",
  "version": "1.0.0",
  "description": "Backend application that allows for the client side application to connect to the database to create users, add profiles and recipes to those users, and retrieve the data if queried with the user ID/recipe ID. The front end part of this application is under construction.",
  "main": "index.js",
  "dependencies": {
    "apollo-server": "^2.9.13",
    "graphql": "^14.5.8",
    "mongoose": "^5.8.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com-personal:arfusop/recipes-backend.git"
  },
  "author": "",
  "license": "ISC"
}
